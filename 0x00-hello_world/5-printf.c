#include <stdio.h>

/**
 * main - this is the beginning of program execution
 *
 * Return: 0 is when successful 
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
